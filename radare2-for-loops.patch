diff -up radare2-3.2.0/libr/asm/p/asm_arm_gnu.c.orig radare2-3.2.0/libr/asm/p/asm_arm_gnu.c
--- radare2-3.2.0/libr/asm/p/asm_arm_gnu.c.orig	2019-01-09 16:46:21.673070915 +0100
+++ radare2-3.2.0/libr/asm/p/asm_arm_gnu.c	2019-01-09 16:47:07.929854934 +0100
@@ -154,7 +154,8 @@ cpucode = 66471;
 		int cpucode = 0;
 		if (a->cpu) {
 			cpucode = atoi (a->cpu);
-			for (int i = 0; i < (sizeof(arm_cpucodes) / sizeof(arm_cpucodes[0])); i++) {
+			int i;
+			for (i = 0; i < (sizeof(arm_cpucodes) / sizeof(arm_cpucodes[0])); i++) {
 				if (!strcmp (arm_cpucodes[i].name, a->cpu)) {
 					cpucode = arm_cpucodes[i].cpucode;
 					break;
 
